LOAD DATABASE
	FROM mysql://root:1234@host.docker.internal:3307/demo_driver360_docker
	INTO postgresql://postgres:1234@host.docker.internal:5433/demo_driver360

WITH include drop, create tables, create indexes, reset sequences, no foreign keys

SET work_mem to '128MB',
	maintenance_work_mem to '128MB'

CAST
	type bigint to bigint drop typemod,
	type datetime to timestamptz,
    type date to date,
    type "tinyint(1)" to boolean drop typemod,
    type "boolean(1)" to boolean drop typemod,
    type boolean to boolean drop typemod

INCLUDING ONLY TABLE NAMES MATCHING 
	~/^cor_vehicle_warranties$/,
	~/^cor_operation_splits_aud$/,
	~/^cor_labours$/,
	~/^cor_mst_cities$/,
	~/^cor_delivery_notes_details$/,
	~/^cor_material_tags$/,
	~/^cor_mst_materials_aud$/,
	~/^cor_materials_sections_aud$/,
	~/^cor_vehicle_warranties_aud$/,
	~/^cor_invoice_reparation_orders$/,
	~/^cor_phones$/,
	~/^cor_gdpr_signatures$/,
	~/^cor_delivery_notes_details_aud$/,
	~/^cor_materials$/,
	~/^cor_mst_material_families$/,
	~/^cor_labours_employees$/,
	~/^superset_or_operations$/,
	~/^cor_reparation_orders_aud$/,
	~/^cor_appointments_aud$/,
	~/^cor_materials_aud$/,
	~/^cor_thirds_aud$/,
	~/^cor_receipts$/,
	~/^ext_agar_imp_vehicles$/,
	~/^cor_appointments$/,
	~/^cor_vehicle_versions$/

set work_mem to '2048 MB', maintenance_work_mem to '4096 MB';